name: kedro_prefect
prefect-version: 2.19.1

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.git_clone:
    repository: git@github.com:OlegPodlipalin/kedro_prefect.git
    branch: main
    access_token: null

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: ci-deployment
  version: null
  tags: []
  description: null
  schedule: {}
  flow_name: my_super_flow
  entrypoint: register_prefect_flow.py:my_flow
  parameters: {}
  work_pool:
    name: docker-poolrerer
    work_queue_name: null
    job_variables:
      image: ghcr.io/olegpodlipalin/my-prefect:latest
      auto_remove: true
      volumes:
        - "/path/on/host:/opt/prefect/data"
